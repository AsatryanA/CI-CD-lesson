pipeline {
  agent any
  stages {
    stage('SonarQube check') {
      steps {
        withSonarQubeEnv(installationName: 'SonarQube') {
            bat 'mvn clean verify sonar:sonar -Dsonar.projectKey=Sonar-Lesson -Dsonar.projectName='Sonar-Lesson''
        }
      }
    }
  }
}
