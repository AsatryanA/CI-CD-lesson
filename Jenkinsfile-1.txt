pipeline {
  agent any
  stages {
    stage('SonarQube check') {
      steps {
        withSonarQubeEnv(installationName: 'Sonar') {
            bat 'mvn clean compile test sonar:sonar'
        }
      }
    }
  }
}
